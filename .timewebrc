{
  "version": 1,
  "build": {
    "command": "pip install -r requirements.txt",
    "dependencies": "requirements.txt"
  },
  "run": {
    "command": "gunicorn --bind 0.0.0.0:8080 --workers 2 main:app"
  }
}

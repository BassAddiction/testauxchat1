# 🚀 Инструкция по запуску AuxChat на Timeweb

## ❗ КРИТИЧЕСКИ ВАЖНО: База данных пустая!

Flask API работает, **НО** база данных не содержит таблиц. Нужно импортировать SQL.

---

## 📋 Пошаговая инструкция

### 1️⃣ Импорт базы данных (ОБЯЗАТЕЛЬНО!)

1. **Скачай** файл `TIMEWEB_IMPORT_SIMPLE.sql` из GitHub репозитория
2. **Открой** Adminer в панели Timeweb: https://89.23.96.125/ (твой адрес из скриншота)
3. **Войди** в базу:
   - Логин: `gen_user`
   - Пароль: (твой пароль из env)
   - База: `default_db`
4. **Нажми** "Импорт" в левом меню
5. **Выбери** файл `TIMEWEB_IMPORT_SIMPLE.sql`
6. **СНИМИ** галочку "Остановить при ошибке"
7. **Нажми** "Выполнить"

✅ **Результат:** Создастся структура + 2 юзера + 20 сообщений

---

### 2️⃣ Перезапуск приложения

После импорта базы:
1. Зайди в **App Platform** Timeweb
2. Найди приложение `auxchat-api`
3. Нажми **"Перезапустить"**
4. Подожди ~30 секунд

---

### 3️⃣ Проверка работы

1. Открой https://api.auxchat.ru/health
   - Должно показать: `{"status": "ok", "message": "AuxChat API Flask", "db_config": {...}}`

2. Открой https://api.auxchat.ru/messages
   - Должно вернуть JSON с сообщениями

3. Открой https://auxchat.ru
   - Чат должен работать!

---

## 🔐 Логин для теста

После импорта базы можешь войти:

**Аккаунт 1:**
- Телефон: `+79221316334`
- Пароль: `123` (или любой, нужно через API создать)

**Аккаунт 2:**
- Телефон: `+79999999999`
- Пароль: `123`

---

## 🐛 Если что-то не работает

### Ошибка "relation messages does not exist"
➡️ **Решение:** Импорт базы не выполнился. Повтори шаг 1️⃣

### Ошибка CORS / 500
➡️ **Решение:** Перезапусти Flask приложение (шаг 2️⃣)

### Не могу войти
➡️ **Решение:** SMS-коды не настроены. Используй регистрацию без верификации или измени код Flask

---

## 📊 Что создаётся при импорте

**Таблицы:**
- `users` — пользователи (2 шт.)
- `messages` — сообщения в общем чате (20 шт.)
- `private_messages` — личные сообщения
- `user_photos` — фотографии профилей
- `subscriptions` — подписки
- `sms_codes` — SMS-коды верификации
- `blacklist` — чёрный список
- `reactions` — реакции на сообщения
- `message_reactions` — дополнительная таблица реакций

**Индексы:** Созданы автоматически для быстрого поиска

---

## ✅ Чеклист запуска

- [ ] Скачал `TIMEWEB_IMPORT_SIMPLE.sql`
- [ ] Импортировал в Adminer
- [ ] Перезапустил Flask приложение
- [ ] Проверил `/health` — возвращает OK
- [ ] Проверил `/messages` — возвращает JSON
- [ ] Открыл auxchat.ru — чат работает
- [ ] Вошёл в аккаунт — всё ОК!

---

**Если всё равно не работает — пиши, починю дальше! 🚀**
